version: '3'
services:
  usb-reset:
    build:
      context: .
      dockerfile: Dockerfile
    image: zdziebek/usb-reset:latest
    restart: no
    volumes:
      - /sys:/sys:rw
      - /dev:/dev:rw
    privileged: true
